---
title: Project
type: project
tags: [project]
status: active
---

## Notes in this project

```query
project: [[{{title}}]]
```

*(the list above updates live as you tag notes into this project — add a `project: [[{{title}}]]` property to any note's frontmatter, of any type, and it shows up here automatically, the same way a Chapter belongs to a Book)*

## Sharing

The **Share** button on this note shares the whole project, not just this page — anyone with the link sees everything listed above too, using whichever role (view/comment/edit) you grant. Add or remove a note from the project any time by editing its `project` property; the share stays live, it doesn't need to be recreated.

## Notes

{{title}} —
